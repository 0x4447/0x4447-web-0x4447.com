export default {
  plugins: [
    // Both - Server and client side plugins
    { src: '~/plugins/both/vee-validate' },
    { src: '~/plugins/both/components' },
    // Only client side plugins
    // Only server side plugins
  ],
}
